# Environment Variables Example
# Copy this file to .env.local for local development overrides

# Worker URL for deployment verification
# Override this if deploying to a custom domain
WORKER_URL=https://energy-genius.YOUR_ACCOUNT_ID.workers.dev

# Note: Worker environment variables are configured in wrangler.toml [vars] section:
# - AI_MODEL_FAST: Fast Llama model for quick responses
# - AI_MODEL_ACCURATE: Accurate Llama model for detailed analysis
# - ENABLE_MOCK_DATA: Use mock data instead of real API calls
# - ENABLE_SSE: Enable Server-Sent Events for streaming responses

# Cloudflare secrets (use wrangler secret put for production):
# - API keys, tokens, and sensitive credentials should NEVER be in wrangler.toml
# - Use: wrangler secret put SECRET_NAME
# Example:
#   wrangler secret put API_KEY
#   wrangler secret put DATABASE_TOKEN
